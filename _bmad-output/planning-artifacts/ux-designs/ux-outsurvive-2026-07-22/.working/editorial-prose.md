| Original Text | Revised Text | Changes |
|---------------|--------------|---------|
| Bốn visual reference load-bearing đã được promote cho Lobby, Match HUD, Inventory và Map/Phase; Pause/Settings, Results và các surface còn lại dựng từ spine. | Bốn visual reference load-bearing cho Lobby, Match HUD, Inventory và Map/Phase đã được promote vào `mockups/`; Pause/Settings, Results và các surface còn lại dựng từ spine. | Clarified what the references belong to and where they were promoted; preserved established contract terms (`DESIGN.md:328`). |
| `health-boost-bar` dưới giữa, `weapon-ammo-panel` phải dưới; subtitle không được giao hai vùng này. | `health-boost-bar` dưới giữa, `weapon-ammo-panel` phải dưới; subtitle không được giao với hai vùng này. | Added the missing preposition required for “giao với,” removing a grammatical ambiguity (`DESIGN.md:350`). |
| Profile có field set GDD.<br>hiển thị đủ field set GDD và huy hiệu không hết hạn | Profile dùng đúng field set được định nghĩa trong GDD.<br>hiển thị đầy đủ field set được định nghĩa trong GDD và huy hiệu không hết hạn | Clarified the relationship between Profile and the GDD-defined field set; deduplicated the same compressed construction at `EXPERIENCE.md:96` and `EXPERIENCE.md:116`. |
| hết server timeout đi theo outcome backend đã khóa | khi timeout của server hết hạn, chuyển theo outcome do backend đã khóa | Restored the missing subject/action relationship while preserving the backend-owned outcome contract (`EXPERIENCE.md:108`). |
| HUD/audio chỉ bằng thông tin target hợp lệ có | HUD/audio chỉ phản ánh thông tin hợp lệ mà target có | Fixed the malformed predicate and made information ownership explicit (`EXPERIENCE.md:114`). |
| Action inventory remappable gồm mọi action được implementation đưa vào gameplay | Danh mục action remappable gồm mọi action được implementation đưa vào gameplay | Removed the ambiguity between an inventory of remappable actions and the in-game Inventory action (`EXPERIENCE.md:187`). |
| Mỗi phát bắn đọc qua tiếng nổ/cơ khí, muzzle flash, vỏ đạn và recoil camera. | Mỗi phát bắn được truyền đạt bằng tiếng nổ/cơ khí, muzzle flash, vỏ đạn và recoil camera. | Replaced the figurative “đọc qua,” which can be parsed literally by an LLM, with an explicit feedback relationship (`EXPERIENCE.md:213`). |
| Mono được kiểm tra không tạo thêm thông tin, nhưng không phải đạt directional metric như stereo. | Mono được kiểm tra để không tạo thêm thông tin, nhưng không bắt buộc đạt directional metric như stereo. | Added the missing purpose marker and modal phrase so the Mono acceptance requirement is unambiguous (`EXPERIENCE.md:225`). |
| `party-panel` cho thấy từng thành viên, voice, ready/network state và cảnh báo trước mọi xung đột quy mô. | Consider: `party-panel` cho thấy từng thành viên, voice, ready/network state và cảnh báo xung đột về quy mô đội trước khi Deploy? | Queried an unclear phrase rather than assuming the intended conflict; the revision states the likely timing and subject without changing the party contract (`EXPERIENCE.md:314`). |
