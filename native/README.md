# Native

Owner: Native integration.

This root is reserved for reviewed native bridges and GDExtensions. Runtime code may
reach native providers only through declared platform ports and adapters; adding a
native dependency requires the architecture, ADR, and five-platform parity gates.
