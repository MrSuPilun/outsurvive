# Backend

Owner: Backend services.

Backend implementations live behind generated contracts and must not import the
Godot game implementation. Generated contract paths are introduced only with the
story that owns their schema and code generation.
